import Nimble
import Quick

internal class ExampleSpec: QuickSpec {
    override internal func spec() {
        describe("The test framework") {
            it("works") {
                expect(true) == true
            }
        }
    }
}
